[tool.poetry]
name = "morphic.py"
version = "2021-Dec-4"
description = ""
authors = ["WarpedWartWars"]

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.1.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
